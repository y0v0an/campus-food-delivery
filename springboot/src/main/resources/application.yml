server:
  port: 8080

file:
  upload-dir: ./img

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.1.1:3306/pt
    username: yyan
    password: 20050119@Wwy

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.community.cfgs.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com.community.cfgs: debug
